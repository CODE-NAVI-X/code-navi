# Optional when the backend is not running at the local default.
NEXT_PUBLIC_CODE_NAVI_API_URL=http://127.0.0.1:8000

# Optional comma-separated hostnames or IP addresses when exposing `next dev`
# to other devices. Leave empty for the portable loopback-only default.
CODE_NAVI_ALLOWED_DEV_ORIGINS=
